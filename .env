# SNMP target (simulator or real device)
SNMP_HOST=127.0.0.1
SNMP_PORT=1161
SNMP_COMMUNITY=demo

# # Which interfaces to poll (comma-separated list of ifIndex)
# SNMP_IF_INDEXES=1

# # Polling interval in seconds
# POLL_INTERVAL_SECONDS=10

# # Database (SQLite for now)
# DATABASE_URL=sqlite:///./metrics.db

# # Use stub mode (fake SNMP data) by default; override to 0 in PowerShell if needed
USE_SNMP_STUB=0
